# HJTPX API 版本控制策略

## 1. 概述

本文档描述了 HJTPX 项目的 API 版本控制策略、迁移计划和最佳实践。

## 2. 版本控制方法

### 2.1 版本标识

- **格式**: 语义化版本 `v{major}`（如 `v1`, `v2`）
- **策略**: 仅在引入破坏性变更时增加主版本号
- **示例**:
  - `v1` - 当前稳定版本
  - `v2` - 下一个主要版本（开发中）

### 2.2 版本指定方式

支持三种方式指定 API 版本（按优先级从高到低）：

1. **URL 路径前缀**（推荐）: `/api/v1/users`
2. **Accept Header**: `Accept: application/vnd.hjtpx.v1+json`
3. **自定义 Header**: `X-API-Version: v1`

### 2.3 默认版本

- 当未指定版本时，默认使用最新稳定版本（当前为 `v1`）

## 3. 版本迁移策略

### 3.1 版本生命周期

| 阶段 | 描述 | 持续时间 |
|------|------|----------|
| 开发 | 新功能开发，可能不稳定 | 直到发布 |
| 稳定 | 推荐使用，接收安全更新 | 至少 12 个月 |
| 弃用 | 仍可用，但建议升级 | 6 个月 |
| 废弃 | 不再维护，移除路由 | - |

### 3.2 版本发布计划

- **稳定版本**: 每 12-18 个月发布一个新主版本
- **弃用警告**: 新版本发布后，旧版本立即标记为弃用
- **废弃时间**: 弃用 6 个月后正式废弃

## 4. 破坏性变更定义

当以下情况发生时，需创建新的主版本：

- 删除或重命名 API 端点
- 删除或重命名请求参数
- 更改响应数据结构
- 更改 HTTP 方法或状态码
- 移除或更改认证方式

## 5. 弃用策略

### 5.1 警告机制

- 在响应头中添加弃用警告:
  - `Warning: 299 - "API v1 is deprecated. Please upgrade to v2"`
  - `X-API-Deprecation-Date: 2026-01-01`
  - `X-API-Sunset-Date: 2026-07-01`

- 在响应体中添加警告信息（可选）

### 5.2 通知机制

- API 文档更新
- 开发人员邮件通知
- 仪表盘警告提示

## 6. 回滚策略

- 保留旧版本路由直到完全废弃
- 支持同时运行多个版本
- 详细的日志记录，便于追踪

## 7. 文档

- 每个版本有独立的 API 文档
- 维护版本变更日志
- 提供迁移指南

## 附录

- [版本迁移指南](./MIGRATION_GUIDE.md)
- [API 变更日志](../CHANGELOG.md)

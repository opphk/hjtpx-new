<view class="text-page">
  <view class="page-header">
    <text class="page-title">文字验证码</text>
    <text class="page-desc">选择正确的文字答案</text>
  </view>

  <view class="captcha-wrapper">
    <view wx:if="{{isLoading}}" class="loading">
      <text>加载中...</text>
    </view>

    <view wx:elif="{{captchaData}}" class="captcha-container">
      <text-captcha
        captchaId="{{captchaData.captchaId}}"
        imageUrl="{{captchaData.imageUrl}}"
        question="{{captchaData.question}}"
        options="{{captchaData.options}}"
        imageSize="{{captchaData.imageSize || {width: 300, height: 150}}}"
        bindverify="onVerify"
      />
    </view>
  </view>

  <view class="action-buttons">
    <button class="refresh-btn" bindtap="onRefresh">
      刷新验证码
    </button>
  </view>

  <view class="result-panel" wx:if="{{isVerified}}">
    <text class="result-text {{verifyResult ? 'success' : 'fail'}}">
      {{verifyResult ? '✓ 验证成功' : '✗ 验证失败'}}
    </text>
  </view>
</view>
